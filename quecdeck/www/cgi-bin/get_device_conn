#!/bin/bash

. /usrdata/quecdeck/www/cgi-bin/cgi-lib.sh
cgi_check_cors
cgi_require_post

cgi_output_text

# Connection-dependent — will error if no active bearer; caller handles absent fields gracefully.
cache_get_or_fetch "$_CACHE_DEVICE_CONN" 3 'AT+QMAP="WWAN";+CGCONTRDP' 2000
