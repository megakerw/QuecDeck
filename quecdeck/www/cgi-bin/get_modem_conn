#!/bin/bash

. /usrdata/quecdeck/www/cgi-bin/cgi-lib.sh
cgi_check_cors
cgi_require_post

cgi_output_text

# Connection-dependent — may fail with no active bearer; caller falls back gracefully.
cache_get_or_fetch "$_CACHE_MODEM_CONN" 3 'AT+QMAP="WWANIP";+CGCONTRDP' 2000
